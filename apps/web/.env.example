# Copy to .env.local (Vite loads it automatically) and fill in.
# .env.local is gitignored.

# Base URL of the RegReview API (apps/server). Locally this is the API's own
# dev port; in production it's wherever the API droplet/container is reachable.
VITE_API_URL=http://localhost:8787
